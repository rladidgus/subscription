# 청약 가점 커트라인 예측 서비스 정리

## 서비스 개요

이 서비스는 청약 아파트 공고 중 **민영분양 일반공급 가점제 공고를 중심으로** 예상 가점 커트라인을 예측하는 서비스입니다.

사용자는 전국 단위 ApplyHome 공고 데이터에서 지역과 단지명을 기준으로 공고를 검색할 수 있습니다. 공고탐색 화면에 표시되는 예측 커트라인은 사용자의 점수와 비교한 결과가 아니라, 해당 공고 자체를 모델에 넣었을 때 나온 **예상 최저 당첨가점**입니다.

즉, 아래 조건을 기준으로 최저 당첨가점이 대략 어느 정도일 가능성이 있는지를 보여줍니다.

- 단지
- 주택형
- 1순위 / 2순위
- 해당지역 거주자 / 기타지역 거주자
- 지역, 공급 규모, 경쟁률, 분양가, 과거 지역 가점 흐름 등 모델 입력 피처

이후 사용자가 원하는 공고를 선택하고 본인의 청약 조건을 입력하면, 서비스는 84점 만점 기준으로 사용자의 청약 가점을 계산한 뒤 예측 커트라인과 비교합니다. 그 결과를 바탕으로 안정권, 가능하지만 불안, 상향 도전, 어려움, 미달 가능, 신청불가 등의 판단 결과를 제공합니다.

## 현재 데이터 기준

현재 공고탐색은 실시간 청약홈 API를 직접 호출하는 방식이 아닙니다. 프로젝트에 저장된 ApplyHome CSV 데이터를 전처리한 로컬 데이터셋을 기준으로 공고를 검색합니다.

주요 데이터 흐름은 다음과 같습니다.

1. ApplyHome 원천 CSV 파일을 프로젝트에 저장합니다.
2. `pipeline/model_features.py`에서 모델 입력용 피처 데이터셋을 생성합니다.
3. 생성된 `data/processed/applyhome_model_features.csv`를 백엔드가 검색합니다.
4. 선택된 공고 행의 피처를 예측 모델에 넣어 커트라인을 계산합니다.

따라서 현재 화면에 보이는 공고는 프로젝트에 포함된 CSV 데이터 기준입니다. 실시간 최신 공고까지 운영 서비스에서 다루려면 청약홈 또는 공공데이터 API 연동이 추가로 필요합니다.

## 사용자 실행 플로우

### 1. 공고탐색

사용자는 공고탐색 화면에서 지역을 선택하거나 단지명을 입력해 공고를 검색합니다.

단지명을 입력하지 않고 지역만 선택해도 해당 지역의 공고가 검색됩니다. 검색 결과에는 단지명, 공고일, 주택형, 지역명, 순위, 거주구분, 예측 커트라인이 표시됩니다.

공고탐색의 예측 커트라인은 개인화 판단이 아닙니다. 이는 공고 자체의 예상 최저 당첨가점입니다.

예시:

```text
예측 커트라인 45점
```

이 값은 해당 단지와 주택형, 순위, 거주구분 조건에서 최저 당첨가점이 약 45점 근처일 가능성이 있다는 의미입니다.

예측 결과가 0점으로 판단되는 경우 화면에는 다음처럼 표시됩니다.

```text
미달 가능
```

이는 0점이어도 무조건 당첨된다는 의미가 아니라, 모델이 해당 공고 조건에서 경쟁 커트라인이 뚜렷하게 형성되지 않거나 미달 가능성이 높다고 판단했다는 의미입니다.

### 2. 공고 선택

사용자가 공고탐색에서 원하는 공고를 선택하면 지원 판단 화면으로 이동합니다.

선택된 공고 정보는 다음 기준으로 유지됩니다.

- 단지명
- 공고번호
- 주택관리번호
- 모델번호
- 주택형
- 지역
- 순위
- 거주구분
- 공고일

지원 판단은 선택한 주택형, 공급순위, 거주구분 기준으로 계산됩니다. 같은 단지라도 주택형, 순위, 거주구분이 다르면 예측 커트라인과 지원 판단 결과가 달라질 수 있습니다.

### 3. 사용자 조건 입력

지원 판단 화면에서 사용자는 본인의 청약 조건을 입력합니다.

현재 입력 항목은 다음과 같습니다.

- 나이
- 혼인 상태
- 무주택 여부
- 무주택 기간
- 부양가족 수
- 청약통장 가입연수
- 청약통장 추가 개월
- 신청 자격 충족 여부

사용자 조건은 직접 숫자를 자유롭게 입력하는 방식이 아니라 선택형 입력으로 구성되어 있습니다. 이는 잘못된 값이 들어가는 것을 줄이고, 청약 가점 계산 기준에 맞춰 값을 입력하기 위함입니다.

유주택으로 선택하면 무주택 기간은 점수에 반영되지 않도록 0년으로 처리됩니다.

### 4. 사용자 청약 가점 계산

백엔드는 `/score/calculate` API에서 사용자 입력값을 바탕으로 청약 가점을 계산합니다.

현재 가점 계산은 84점 만점 구조입니다.

- 무주택 기간: 최대 32점
- 부양가족 수: 최대 35점
- 청약통장 가입기간: 최대 17점

계산된 점수는 지원 판단 화면에서 사용자 점수로 표시됩니다.

### 5. 지원 판단 실행

지원 판단 실행 시 백엔드는 선택한 공고의 예측 커트라인과 사용자의 청약 점수를 비교합니다.

판단 결과는 다음과 같은 등급으로 제공됩니다.

- 미달 가능
- 지원 최적
- 안전권
- 가능하지만 불안
- 상향 도전
- 어려움
- 신청 불가

신청 자격을 충족하지 않는다고 사용자가 선택하면 모델 예측 전에 신청불가로 분류됩니다.

## 예측 모델 설명

### 모델 목적

예측 모델의 목적은 ApplyHome 공고 행을 입력받아 해당 공고 조건의 **예상 최저 당첨가점**을 계산하는 것입니다.

모델의 타깃 값은 `LWET_SCORE`입니다. 이는 ApplyHome 당첨가점 데이터에서 최저 당첨가점을 의미합니다.

### 모델 입력 피처

모델은 ApplyHome 원천 데이터를 전처리해 만든 22개 피처를 사용합니다.

현재 모델 메타데이터 기준 피처는 다음과 같습니다.

```text
log_cmpet_rate
SUPLY_HSHLDCO
spsply_ratio
SUBSCRPT_RANK_CODE
RESIDE_SECD
SPECLT_RDN_EARTH_AT
MDAT_TRGET_AREA_SECD
PARCPRC_ULS_AT
IMPRMN_BSNS_AT
PUBLIC_HOUSE_EARTH_AT
LRSCL_BLDLND_AT
NPLN_PRVOPR_PUBLIC_HOUSE_AT
log_lttot_top_amount
house_area
is_top_brand
price_rank_in_region
cmpet_rank_in_region
region_score_lag1
region_score_roll5_mean
region_score_roll5_std
notice_year
notice_month
```

피처는 크게 다음 정보를 반영합니다.

- 경쟁률
- 일반공급 세대수
- 특별공급 비율
- 청약 순위
- 해당지역 / 기타지역 거주구분
- 투기과열지구, 조정대상지역 등 지역 규제 관련 값
- 공공주택 관련 여부
- 대규모 택지 여부
- 분양가
- 주택형 면적
- 주요 브랜드 여부
- 지역 내 분양가 상대 순위
- 지역 내 경쟁률 상대 순위
- 과거 같은 지역의 최저 당첨가점 흐름
- 공고 연도와 월

### 모델 구조

현재 서비스는 LightGBM 기반 Hybrid 구조를 사용합니다.

모델 파일은 `model_artifacts/final/` 아래에 저장되어 있습니다.

- `lgb_tuned.txt`
- `hurdle_classifier.txt`
- `seg_classifier_30.txt`
- `seg_model_low.txt`
- `seg_model_high.txt`

예측 흐름은 다음과 같습니다.

1. 먼저 `hurdle_classifier`가 해당 공고가 경쟁 커트라인이 형성될 가능성이 있는지 판단합니다.
2. 경쟁 가능성이 낮으면 예측값을 0점으로 보고, 서비스 화면에는 `미달 가능`으로 표시합니다.
3. 경쟁 가능성이 높으면 구간별 모델을 사용합니다.
4. `seg_classifier_30`이 30점 이상 고득점 구간 가능성을 추정합니다.
5. `seg_model_low`와 `seg_model_high`가 각각 저점 구간과 고점 구간 예측값을 계산합니다.
6. 두 예측값을 확률 기반으로 섞어 최종 커트라인을 계산합니다.

코드에서는 이 방식을 `LightGBM-Hybrid`로 사용합니다.

간단히 표현하면 다음과 같습니다.

```text
if 경쟁 발생 확률 < 0.5:
    예측 커트라인 = 0
    표시 = 미달 가능
else:
    고점구간 확률 = segment_classifier(X)
    저점 예측 = segment_low(X)
    고점 예측 = segment_high(X)
    최종 예측 = 고점구간 확률 * 고점 예측 + (1 - 고점구간 확률) * 저점 예측
```

예측값은 청약 가점 범위에 맞춰 0점에서 84점 사이로 제한됩니다.

### 모델 성능 기준

모델 메타데이터 기준 주요 성능은 다음과 같습니다.

- 단일 회귀 최고 모델: `V0. LGB tuned`
- 단일 회귀 MAE: 약 6.23점
- 운영 기준 모델: `V2. Hurdle`
- 운영 기준 MAE: 약 7.28점

단일 회귀 모델은 경쟁이 발생한 공고의 점수 예측에는 강점이 있습니다. 하지만 실제 서비스에서는 0점 또는 미달 가능성이 있는 공고도 함께 다뤄야 하므로, Hurdle 구조를 포함한 Hybrid 방식이 운영 흐름에 더 적합합니다.

### 지역별 신뢰도

백엔드는 `model_artifacts/reports/region_performance.csv`에 저장된 지역별 성능 리포트를 읽어 지역별 평균 오차와 신뢰도 정보를 계산합니다.

다만 현재 프론트 화면에서는 사용자가 보기 쉽게 만들기 위해 지역 평균 오차와 모델 신뢰도 값을 직접 노출하지 않습니다. 내부 판단에서는 지역별 오차 정보를 사용해 사용자의 점수와 예측 커트라인 사이의 여유를 해석합니다.

## 지원 판단 등급 기준

지원 판단은 사용자의 청약 가점과 모델이 예측한 커트라인의 차이를 기준으로 계산합니다.

### 미달 가능

모델이 해당 공고를 미달 또는 0점 가능성이 높은 공고로 판단한 경우입니다.

이 경우 화면에는 예측 커트라인을 `0점`으로 표시하지 않고 `미달 가능`으로 표시합니다.

### 지원 최적

사용자의 점수가 예측 커트라인보다 충분히 높은 경우입니다. 내부적으로는 점수 여유가 지역 평균 오차의 두 배 이상일 때 해당 등급으로 분류됩니다.

### 안전권

사용자의 점수가 예측 커트라인보다 높고, 점수 여유가 지역 평균 오차보다 큰 경우입니다.

### 가능하지만 불안

사용자 점수가 예측 커트라인보다 높지만, 차이가 오차 범위 안쪽인 경우입니다.

### 상향 도전

사용자 점수가 예측 커트라인보다 낮지만, 차이가 오차 범위 안쪽인 경우입니다.

### 어려움

사용자 점수가 예측 커트라인보다 낮고, 차이가 오차 범위를 벗어나는 경우입니다.

### 신청 불가

사용자가 신청 자격을 충족하지 않는다고 입력한 경우입니다. 이때는 모델 예측 전에 규칙 기반으로 제외됩니다.

## 주요 API

### 점수 계산

```text
POST /score/calculate
```

사용자의 청약 조건을 입력받아 84점 만점 기준 청약 가점을 계산합니다.

### 공고별 커트라인 예측

```text
POST /predict/applyhome-cutoff
```

공고탐색에서 사용합니다. 지역, 단지명, 주택형, 순위, 거주구분 등의 조건으로 공고를 찾고, 해당 공고의 예상 커트라인을 반환합니다.

### 사용자 점수 기반 지원 판단

```text
POST /predict/applyhome-classify
```

사용자 점수와 선택한 공고를 비교해 지원 결과를 분류합니다.

## 현재 서비스의 한계

현재 서비스는 실시간 청약 공고 API가 아니라 로컬 CSV 데이터 기반으로 동작합니다. 따라서 최신 공고가 CSV에 없으면 검색 결과에도 나타나지 않습니다.

또한 서비스 방향은 민영분양 일반공급 가점제 중심이지만, ApplyHome 데이터에는 공공주택 관련 플래그도 포함되어 있습니다. 운영 서비스에서 더 정확히 구분하려면 민영분양, 공공분양, 임대, 분양전환 등 공고 유형 필터를 별도로 강화하는 것이 좋습니다.

공공분양은 납입횟수, 납입인정금액, 소득, 자산, 특별공급 조건 등이 중요한 경우가 많습니다. 따라서 공공분양까지 정확히 지원하려면 현재의 84점 가점제 모델과 별도의 판단 로직이 필요합니다.

## 향후 개선 방향

- 실시간 청약홈 또는 공공데이터 API 연동
- 공고 유형 필터 추가
- 민영분양 일반공급 가점제 대상 공고만 명확히 분리
- 공공분양 전용 판단 로직 분리
- 공고별 부족 피처에 대한 보정 로직 추가
- 사용자에게 예측 불확실성을 더 이해하기 쉬운 문구로 제공
